{
  "name": "json2graphite",
  "version": "1.0.0",
  "description": "Send JSON data to Graphite",
  "main": "index.js",
  "bin": "./bin/json2graphite",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pkg": "pkg --out-path build package.json"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "get-stdin": "^6.0.0",
    "graphite": "^0.1.2",
    "lodash": "^4.17.10",
    "yargs": "^12.0.1"
  },
  "devDependencies": {
    "mocha": "^5.2.0"
  }
}
